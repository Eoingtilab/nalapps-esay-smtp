# Tests

NalApps Easy SMTP is validated through the CI quality gates in
`.github/workflows/` (PHP syntax across the supported PHP matrix, WPCS,
WordPress Plugin Check, dependency audit, and product-contract grep
assertions in `quality.yml` / `release.yml`) rather than a bundled
PHPUnit suite.
